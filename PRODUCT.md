# 角色卡管理器产品说明

## Product Purpose

角色卡管理器是面向 SillyTavern / TauriTavern 用户的角色卡维护工具。它将用于集中处理角色卡检查、预览、整理、导入辅助、风险提示和后续可能的批量维护流程。用户通常已经处在角色卡、世界书和聊天上下文中，因此界面必须优先服务于判断、预览和确认。

当前仓库处于准备阶段：先建立工程、规则、设计和发布流程，不实现完整角色卡管理功能。

## Users

- 经常导入、更新、整理角色卡的 SillyTavern 用户。
- 需要检查角色设定、内嵌世界书、脚本、变量和资源状态的高频维护者。
- 在桌面或移动端 TauriTavern 环境里排查角色卡兼容性和导入结果的用户。

## Product Principles

- 先预览，后应用。任何会修改、覆盖、删除或批量处理用户数据的操作，都必须先给出明确方案，再由用户确认。
- 保守处理用户数据。默认策略应减少误删、误改、绑定丢失和脚本污染风险；低置信结果需要明显提示。
- 信息密度高但结构稳定。用户需要快速扫描角色卡、字段、风险、差异和预览结果，布局应保持可预测。
- 与宿主环境共存。界面使用独立前缀、较高 z-index 和深色工具面板，但不能依赖宿主页面视觉。
- 真实状态优先。API 不可用、读取失败、解析失败、保存失败都应直接提示，不能静默吞掉。
- 参考旧脚本，但不复制旧脚本。`参考资料/参考用脚本/角色卡管理器参考脚本.json` 只作为后续理解需求的材料。

## Core Workflows

- 准备阶段：建立模板工程、文档、分支、发布流程和空脚本入口。
- 后续功能阶段：围绕角色卡读取、检查、预览、确认和应用逐步设计。
- 后续发布阶段：从 `dev` 准备 release，经 `main` 触发 bundle，再将 tag / Release 指向 `[bot] bundle`。

## UX Boundaries

- 不做落地页、欢迎页或装饰性介绍页，打开后应直接进入工具任务。
- 不把不可逆操作藏在单击按钮后，删除、覆盖和批量修改必须走确认。
- 不用花哨动效或大面积色彩表达功能，颜色只用于状态、选中、风险和主操作。
- 不为单个功能发明独立视觉系统，新面板必须参照 `DESIGN.md` 的统一布局和组件节奏。
