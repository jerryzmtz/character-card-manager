# 角色卡管理器设计规范

## Design Source

当前设计规范继承世界书管理器的产品工具理念：深色、克制、信息密集、先配置后预览、先确认后应用。后续实现时，应优先复用模板能力和稳定的 `角色卡管理器` 独立样式前缀，避免与 SillyTavern / TauriTavern 宿主页面混杂。

## Visual Register

- 类型：深色产品工具界面。
- 使用场景：用户在 SillyTavern / TauriTavern 内维护已有角色卡，需要扫描、比较、确认和回退。
- 气质：克制、清晰、可操作。避免营销感、装饰感和玩具感。

## Layout

- 主弹窗使用居中 overlay，保留足够最大宽度和最大高度，内容区域内部滚动。
- 顶部为 sticky header，左侧显示面板标题和版本，右侧放设置、说明、刷新、关闭等图标按钮。
- 配置区优先使用双列网格：
  - 左列为规则、模式、策略或说明区。
  - 右列为角色卡、字段、资源或候选列表。
  - 左列约 `300px-360px`，右列占剩余空间。
- 移动端和窄屏改为单列：
  - 规则区在上。
  - 列表和预览在下。
  - 操作区保持可滚动、可折叠。
- 预览区位于配置区下方，承载建议、风险、差异和应用按钮。

## Components

- 面板：深色表面、细边框、紧凑内边距，避免卡片套卡片。
- 按钮：主操作、次操作、危险操作、图标操作要有稳定层级，不用不同形态表达同一层级操作。
- 图标：优先用于设置、刷新、说明、关闭、风险提示和状态，不用图标替代关键风险文案。
- 输入和选择控件：高度、边框、focus 样式保持统一。
- 列表：用于角色卡、字段、资源、脚本和风险项时，保持行高稳定、状态标签清楚。
- 风险和提示：必须在预览区或结果区可见，不把关键风险藏在 tooltip 或日志里。

## Color And Tokens

- 新增样式应使用项目统一 token 或独立前缀变量。
- 主背景为深色，面板略高于背景，边框只用于分区和交互边界。
- 文本分为主文本、次要说明、弱提示和禁用态。
- 主操作使用蓝色语义；成功、警告、危险分别使用稳定的绿、黄、红语义。
- 不新增装饰性渐变、玻璃拟态、大面积单色主题或高饱和装饰色。

## Typography

- 使用系统 UI 字体栈，保持工具的原生感。
- 面板标题紧凑，正文和说明使用小字号但不低于可读标准。
- 按钮和标签文案短句优先，避免长段说明塞进控件。
- 提示文案要说明影响和风险，不重复标题。

## Interaction Rules

- 修改、覆盖、删除和批量处理必须先生成方案，再由用户确认。
- 低置信、跨字段、脚本执行、资源缺失、导入覆盖等风险必须在预览中可见。
- 设置变更可以即时保存到 localStorage，但不自动执行数据修改。
- 加载、读取失败、应用失败必须可见反馈。
- 新功能应保持配置区、列表区、预览区、确认区的固定节奏。

## Anti Patterns

- 不做 landing page。
- 不使用嵌套卡片堆叠规则说明。
- 不让视觉装饰压过数据判断。
- 不把用户需要确认的风险藏在 tooltip、控制台或折叠日志里。
- 不默认安装深度请求 hook 作为核心路径。
